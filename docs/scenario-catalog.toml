schema = "relay.woooo.tech/schemas/planning-scenarios-v1"
status = "accepted"

# IDs are SC-G<two-digit gate>-<priority>-<two-digit first plus case index>.

[[scenario_set]]
owner_task = "T-G00-01"
gate = "G0"
priority = "P0"
first = 1
verification_id = "VERIFY-G00-01"
kind = "documentation"
cases = [
  { title = "Ratify identity and TLS channel binding", acceptance = "Accepted ADR fixes canonical NodeId and ClusterId syntax, immutable Ed25519 bindings, TLS 1.3 CertificateVerify validation, RFC 9266 exporter binding, role-separated transcript proofs, and fail-closed collision handling.", threats = [
    "THR-001",
    "THR-002",
    "THR-003",
    "THR-004",
    "THR-013",
  ] },
  { title = "Ratify single-use admission and member recovery", acceptance = "Accepted ADR fixes ten-minute memory-only credentials, one committed subject per generation, committed/aborted/unknown reconciliation, lost-response recovery through member authentication, credential-free reconnect, and fail-closed key loss.", threats = [
    "THR-001",
    "THR-002",
    "THR-004",
    "THR-005",
    "THR-013",
    "THR-015",
  ] },
]

[[scenario_set]]
owner_task = "T-G00-02"
gate = "G0"
priority = "P0"
first = 3
verification_id = "VERIFY-G00-02"
kind = "documentation"
cases = [
  { title = "Ratify bounded deterministic wire decoding", acceptance = "Accepted ADR fixes the 16-byte prelude, opaque schema and kind IDs, deterministic CBOR rules, tag grammar, body and collection limits, and rejection before oversized allocation or dispatch.", threats = [
    "THR-009",
    "THR-014",
  ] },
  { title = "Ratify authenticated feature intersection", acceptance = "Accepted ADR fixes signed offers, definition digests, dependency closure, conflict rejection, required-label handling, minimum numeric limits, exact selection bytes, and no automatic fallback.", threats = [
    "THR-003",
    "THR-014",
  ] },
  { title = "Ratify bounded durable delivery", acceptance = "Accepted ADR fixes one TraceId, source-signed ordinals 0 through 3, 15-hop DATA and receipt limits, persist-before-write, destination acceptance before ACK, duplicate re-ACK, quotas, and 24-hour conservative retention.", threats = [
    "THR-002",
    "THR-009",
    "THR-011",
    "THR-020",
  ] },
]

[[scenario_set]]
owner_task = "T-G00-03"
gate = "G0"
priority = "P0"
first = 6
verification_id = "VERIFY-G00-03"
kind = "documentation"
cases = [
  { title = "Ratify conditional transaction reconciliation", acceptance = "Accepted ADR fixes immutable snapshots, unsigned-byte scans, base-revision and per-key CAS, cross-family atomic batches, TxnId receipts, committed/aborted/unknown outcomes, locked reopen reconciliation, and capability refusal.", threats = [
    "THR-020",
    "THR-021",
  ] },
  { title = "Ratify HLC quarantine migration and cleanup rules", acceptance = "Accepted ADR fixes checked HLC transitions, persisted watermarks, clock-health rules, five-second future quarantine, conservative durable age, immutable migration graphs, never-automatic tombstone deletion, and exact-CAS acknowledged cleanup.", threats = [
    "THR-011",
    "THR-022",
    "THR-023",
  ] },
]

[[scenario_set]]
owner_task = "T-G00-04"
gate = "G0"
priority = "P1"
first = 1
verification_id = "VERIFY-G00-04"
kind = "documentation"
cases = [
  { title = "Ratify toolchain feature and dependency policy", acceptance = "Accepted ADR and Cargo metadata fix Rust 1.97.1, publish=false during development, additive json and redb features, unconditional security behavior, approved package families, locked resolutions, and one TLS provider.", threats = [
    "THR-027",
    "THR-029",
  ] },
  { title = "Ratify evidence budgets and secret-safe replay", acceptance = "Accepted ADR fixes property, simulation, fuzz, crash, soak, and release budgets; reviewed corpus promotion; bounded allowlisted artifacts; closed replay argv; no automatic reruns; and exact-SHA release-token validation.", threats = [
    "THR-013",
    "THR-024",
    "THR-025",
    "THR-026",
    "THR-027",
  ] },
]

[[scenario_set]]
owner_task = "T-G00-05"
gate = "G0"
priority = "P1"
first = 3
verification_id = "VERIFY-G00-05"
kind = "documentation"
cases = [
  { title = "Ratify the isolated sixteen-node OCI profile", acceptance = "Accepted ADR fixes the Linux host class, OCI engine lineage, container limits, redb volumes, two-network isolation, unimpaired bridge probes, public-facade harness boundary, exact 32-edge topology, clocks, readiness, and quiescence.", threats = [
    "THR-028",
  ] },
  { title = "Ratify the complete 125-sample SLO ledger", acceptance = "Accepted ADR fixes five fresh runs, 25 ordered samples per run, deterministic 4 KiB payloads, concurrent public polling, elapsed time at most 10000 ms for every sample, predeclared IDs, raw observations, and no post-start exclusion or replacement.", threats = [
    "THR-026",
    "THR-027",
    "THR-028",
  ] },
]

[[scenario_set]]
owner_task = "T-G00-06"
gate = "G0"
priority = "P0"
first = 8
verification_id = "VERIFY-G00-06"
kind = "validation"
cases = [
  { title = "Validate immutable scenario threat and API ownership", acceptance = "Planning validation rejects duplicate or malformed SC and THR IDs, reused IDs, missing scenario owners, duplicate constant owners, absent API signatures, and task references to unregistered scenarios.", threats = [
  ] },
  { title = "Validate mitigation and evidence-impact coverage", acceptance = "Planning validation proves every THR-001 through THR-029 has a mitigation or named residual risk, every scenario has verification ownership, every evidence-affecting production path is mapped, and every later task is dispatchable by its registered verification ID.", threats = [
    "THR-001",
    "THR-002",
    "THR-003",
    "THR-004",
    "THR-005",
    "THR-006",
    "THR-007",
    "THR-008",
    "THR-009",
    "THR-010",
    "THR-011",
    "THR-012",
    "THR-013",
    "THR-014",
    "THR-015",
    "THR-016",
    "THR-017",
    "THR-018",
    "THR-019",
    "THR-020",
    "THR-021",
    "THR-022",
    "THR-023",
    "THR-024",
    "THR-025",
    "THR-026",
    "THR-027",
    "THR-028",
    "THR-029",
  ] },
]

[[scenario_set]]
owner_task = "T-G01-01"
gate = "G1"
priority = "P0"
first = 1
verification_id = "VERIFY-G01-01"
kind = "property"
cases = [
  { title = "Reject hostile envelopes before excess allocation", acceptance = "Properties reject invalid magic, lengths, flags, reserved bits, schemas, kinds, nesting, collections, duplicate fields, trailing values, and bodies above the prechecked limit without panic or allocation beyond the configured bound.", threats = [
    "THR-009",
    "THR-014",
  ] },
]

[[scenario_set]]
owner_task = "T-G01-01"
gate = "G1"
priority = "P1"
first = 1
verification_id = "VERIFY-G01-01"
kind = "property"
cases = [
  { title = "Enforce canonical ID and tag namespaces", acceptance = "Generated valid NodeId, ClusterId, TraceId, and qualified tags round-trip byte-for-byte; invalid prefix, length, alphabet, case, whitespace, DNS, category, separator, or reserved-namespace inputs are rejected.", threats = [
    "THR-004",
    "THR-014",
  ] },
  { title = "Encode deterministic prelude and CBOR bytes", acceptance = "Equivalent envelopes produce identical prelude and deterministic-CBOR bytes; golden vectors remain stable and non-shortest integers, indefinite items, duplicate keys, invalid key order, floats, and undeclared tags are rejected.", threats = [
    "THR-009",
    "THR-014",
  ] },
]

[[scenario_set]]
owner_task = "T-G01-02"
gate = "G1"
priority = "P0"
first = 2
verification_id = "VERIFY-G01-02"
kind = "integration"
cases = [
  { title = "Own node startup and shutdown lifecycle", acceptance = "The public builder starts one supervised runtime, cloned handles share it, shutdown is idempotent, wait reports termination, cancellation drains owned tasks, and no task, queue, listener, or placeholder add API remains after shutdown.", threats = [
  ] },
]

[[scenario_set]]
owner_task = "T-G01-02"
gate = "G1"
priority = "P1"
first = 3
verification_id = "VERIFY-G01-02"
kind = "contract"
cases = [
  { title = "Drive time and randomness through injected facades", acceptance = "Clock sleeps and monotonic reads advance deterministically under virtual time, entropy requests consume reproducible bytes, checked time arithmetic rejects overflow, and production lifecycle code has no ambient timing or randomness dependency.", threats = [
    "THR-011",
  ] },
]

[[scenario_set]]
owner_task = "T-G01-03"
gate = "G1"
priority = "P0"
first = 3
verification_id = "VERIFY-G01-03"
kind = "simulation"
cases = [
  { title = "Replay a bounded directed network exactly", acceptance = "For each retained seed, the simulator produces the same ordered events and digest while enforcing directed-link state, virtual deadlines, bounded queues, and no wall-clock sleep.", threats = [
  ] },
]

[[scenario_set]]
owner_task = "T-G01-03"
gate = "G1"
priority = "P1"
first = 4
verification_id = "VERIFY-G01-03"
kind = "simulation"
cases = [
  { title = "Model the complete deterministic fault matrix", acceptance = "Across 1000 gate seeds, scenarios independently and jointly exercise delay, loss, duplication, reorder, one-way links, partitions, healing, restart, and address change while preserving configured bounds and deterministic replay.", threats = [
  ] },
]

[[scenario_set]]
owner_task = "T-G01-04"
gate = "G1"
priority = "P0"
first = 4
verification_id = "VERIFY-G01-04"
kind = "security"
cases = [
  { title = "Reject secrets and bound failure artifacts", acceptance = "Artifact construction rejects every forbidden credential, key, handle, proof, exporter, transcript, payload, address, path, environment, and hostile-text field before hashing; event and byte limits produce deterministic first/last truncation.", threats = [
    "THR-013",
    "THR-024",
  ] },
  { title = "Replay failures without command injection", acceptance = "Replay accepts only allowlisted executable IDs and validated literal argv, rejects shell syntax, hostile paths and oversized arguments, normalizes ephemeral values, and emits byte-identical simulation artifacts for the same seed and manifest.", threats = [
    "THR-024",
  ] },
]

[[scenario_set]]
owner_task = "T-G01-05"
gate = "G1"
priority = "P0"
first = 6
verification_id = "VERIFY-G01-05"
kind = "closure"
cases = [
  { title = "Close the public foundation facade", acceptance = "An external-crate test uses only manifest reexports to construct and shut down a node; private protocol, runtime, storage, and simulator types remain inaccessible and the placeholder add export is absent.", threats = [
  ] },
  { title = "Close MSRV stable dependency and lint gates", acceptance = "Rust 1.97.1 and current stable pass locked Q with zero warnings, cargo-deny accepts the reviewed graph, production unwrap and expect are denied, and CI rejects unlocked or policy-violating dependencies.", threats = [
    "THR-025",
    "THR-029",
  ] },
]

[[scenario_set]]
owner_task = "T-G02-01"
gate = "G2"
priority = "P0"
first = 1
verification_id = "VERIFY-G02-01"
kind = "contract"
cases = [
  { title = "Enforce conditional atomic storage semantics", acceptance = "The reusable contract proves immutable snapshots, exact lookup, empty and all-0xff prefix ordering, base-revision and per-key conflicts, cross-family all-or-nothing batches, TxnId idempotence and digest conflict, and exact committed, aborted, and unknown reconciliation.", threats = [
    "THR-020",
    "THR-021",
  ] },
]

[[scenario_set]]
owner_task = "T-G02-01"
gate = "G2"
priority = "P1"
first = 1
verification_id = "VERIFY-G02-01"
kind = "contract"
cases = [
  { title = "Retain receipts while durable references exist", acceptance = "Receipt cleanup conflicts while any owner record, pending operation, provider intent, migration, or unknown outcome references its TxnId; an old unreferenced receipt is removed only after conservative age and cannot authorize TxnId reuse.", threats = [
    "THR-020",
    "THR-023",
  ] },
]

[[scenario_set]]
owner_task = "T-G02-02"
gate = "G2"
priority = "P0"
first = 2
verification_id = "VERIFY-G02-02"
kind = "security"
cases = [
  { title = "Commit unique identity genesis and admission records", acceptance = "Properties and fault cases prove immutable IdentityBinding keys, one ClusterGenesis on an empty store, unique CredentialUse and AdmissionGrant keys, exact indeterminate-commit reconciliation, and no second subject for one credential generation.", threats = [
    "THR-004",
    "THR-005",
    "THR-015",
    "THR-020",
  ] },
  { title = "Keep key-provider material secret and referenced", acceptance = "Provider creation returns only an opaque durable handle and public key; restart verifies the recorded key, missing or mismatched keys stop networking, diagnostics redact private material, and no definitely or indeterminately referenced handle is deleted.", threats = [
    "THR-004",
    "THR-013",
    "THR-015",
    "THR-020",
  ] },
]

[[scenario_set]]
owner_task = "T-G02-03"
gate = "G2"
priority = "P0"
first = 4
verification_id = "VERIFY-G02-03"
kind = "integration"
cases = [
  { title = "Persist immutable checksummed JSON generations", acceptance = "The JSON adapter holds one alias-safe lifetime lock, writes a deterministic complete generation to a unique same-directory name, validates the full parent and checksum chain on reopen, reports its exact capability level, and never overwrites a final generation.", threats = [
    "THR-020",
    "THR-021",
  ] },
]

[[scenario_set]]
owner_task = "T-G02-03"
gate = "G2"
priority = "P1"
first = 2
verification_id = "VERIFY-G02-03"
kind = "platform"
cases = [
  { title = "Build and test every complete feature combination", acceptance = "Pinned cargo-hack check and test pass for no-default, default json, redb-only when available, and all-feature combinations; json remains additive and test-only, and no feature disables authentication, bounds, canonical decoding, or redaction.", threats = [
    "THR-025",
    "THR-029",
  ] },
]

[[scenario_set]]
owner_task = "T-G02-04"
gate = "G2"
priority = "P0"
first = 5
verification_id = "VERIFY-G02-04"
kind = "crash"
cases = [
  { title = "Recover JSON commits at every durability boundary", acceptance = "Subprocess faults before and after create, write, file flush, unique rename, directory barrier, result delivery, cleanup deletion, and cleanup barrier reopen to the exact old, new, or unresolved state; committed receipts remain reconcilable and every final chain stays valid.", threats = [
    "THR-015",
    "THR-020",
    "THR-021",
  ] },
  { title = "Recover key creation and deletion intents safely", acceptance = "Faults at every provider and storage boundary preserve creation or deletion evidence, reconcile Present, Absent, or Unknown outcomes idempotently, prohibit new references after a deletion intent, and never delete a handle referenced by any committed generation.", threats = [
    "THR-004",
    "THR-013",
    "THR-020",
    "THR-021",
    "THR-023",
  ] },
]

[[scenario_set]]
owner_task = "T-G02-05"
gate = "G2"
priority = "P0"
first = 7
verification_id = "VERIFY-G02-05"
kind = "security"
cases = [
  { title = "Fail closed on native lock corruption and path faults", acceptance = "Native tests reject concurrent and aliased opens, invalid highest generations, duplicate or broken chains, unknown schemas, permission failures, unsafe directory barriers, failed deletion cleanup, and unrelated-file mutation while releasing the lock after process exit.", threats = [
    "THR-021",
  ] },
]

[[scenario_set]]
owner_task = "T-G02-05"
gate = "G2"
priority = "P1"
first = 3
verification_id = "VERIFY-G02-05"
kind = "platform"
cases = [
  { title = "Enforce platform capability claims in CI", acceptance = "Linux, macOS, and Windows execute the native JSON suite; unsupported OS-crash durability returns UnsupportedCapability, Unix claims require an observed directory barrier, and workflow evidence includes act when available or complete local Q plus a passing pushed run without rerun masking.", threats = [
    "THR-021",
    "THR-026",
    "THR-029",
  ] },
]
[[scenario_set]]
owner_task = "T-G03-01"
gate = "G3"
priority = "P0"
first = 1
verification_id = "VERIFY-G03-01"
kind = "security"
cases = [
  { title = "reject invalid feature registry graphs", acceptance = "registry construction rejects duplicate labels, missing dependencies, dependency cycles, asymmetric or self conflicts, and duplicate limit ownership before network I/O", threats = [
    "THR-014",
  ] },
  { title = "reject mutated feature definitions", acceptance = "handshake rejects any changed fingerprint, dependency, conflict, limit, handler owner, definition digest, or reserved namespace while accepting the frozen fixture unchanged", threats = [
    "THR-007",
    "THR-014",
  ] },
  { title = "freeze canonical authentication vectors", acceptance = "both roles produce the exact golden transcript and selection bytes for canonical offers, and any field or ordering mutation fails verification", threats = [
    "THR-002",
    "THR-007",
    "THR-014",
  ] },
  { title = "reject malformed handshake state transitions", acceptance = "join and member handshakes reject duplicate, unknown mandatory, out-of-order, role-swapped, identity-equal, or non-canonical messages without signing or admitting", threats = [
    "THR-002",
    "THR-003",
    "THR-014",
  ] },
]

[[scenario_set]]
owner_task = "T-G03-02"
gate = "G3"
priority = "P0"
first = 5
verification_id = "VERIFY-G03-02"
kind = "integration"
cases = [
  { title = "complete a real exporter-bound WSS join", acceptance = "two loopback nodes complete TLS 1.3 with CertificateVerify validation, derive the same RFC 9266 exporter locally, authenticate both identities, and persist admission", threats = [
    "THR-003",
  ] },
  { title = "serve a direct request only after authentication", acceptance = "a manifest-defined request succeeds over real WSS after mutual authentication, while application frames before authentication and frames from an unauthenticated replacement connection are rejected", threats = [
    "THR-003",
    "THR-004",
  ] },
]

[[scenario_set]]
owner_task = "T-G03-03"
gate = "G3"
priority = "P0"
first = 7
verification_id = "VERIFY-G03-03"
kind = "fault"
cases = [
  { title = "reconcile definitely aborted admission commits", acceptance = "faulting every pre-commit boundary leaves binding, credential use, and grant all absent and releases the still-valid credential generation for one later attempt", threats = [
    "THR-005",
    "THR-015",
  ] },
  { title = "quarantine unknown admission outcomes", acceptance = "every indeterminate commit result blocks credential reuse, rotation, signing, and networking until authoritative reopen finds the exact complete transaction or proves absence", threats = [
    "THR-005",
    "THR-015",
  ] },
  { title = "recover a committed admission after response loss", acceptance = "dropping the final response still permits the same identity to recover its stored grant through member authentication, and no schedule commits two subjects for one issuer generation", threats = [
    "THR-002",
    "THR-005",
    "THR-015",
  ] },
]

[[scenario_set]]
owner_task = "T-G03-04"
gate = "G3"
priority = "P0"
first = 10
verification_id = "VERIFY-G03-04"
kind = "property"
cases = [
  { title = "select identical features across offer permutations", acceptance = "all supported and required offer orderings produce one canonically sorted dependency-closed intersection with byte-identical selections at both peers", threats = [
    "THR-007",
    "THR-014",
  ] },
  { title = "handle unknown feature labels without fallback", acceptance = "an unknown optional label remains transcript-bound but unselected, while an unknown required label closes the connection without retrying a weaker offer", threats = [
    "THR-007",
  ] },
  { title = "reject unsatisfied dependencies and conflicts", acceptance = "fixed-point dependency pruning is deterministic and authentication fails whenever a required label is pruned or a conflict pair remains selected", threats = [
    "THR-007",
    "THR-014",
  ] },
  { title = "negotiate numeric limits at exact boundaries", acceptance = "legal floor and ceiling offers select their exact minimum, while missing, duplicate, unknown mandatory, or out-of-range limits reject before session activation", threats = [
    "THR-007",
    "THR-009",
    "THR-014",
  ] },
  { title = "reconnect without credential or downgrade", acceptance = "after credential rotation and disconnect, both nodes reconnect using fresh exporter-bound member proofs with the prior exact feature policy and never consult a join credential or weaken the offer", threats = [
    "THR-002",
    "THR-003",
    "THR-007",
  ] },
]

[[scenario_set]]
owner_task = "T-G03-05"
gate = "G3"
priority = "P0"
first = 15
verification_id = "VERIFY-G03-05"
kind = "concurrency"
cases = [
  { title = "multiplex concurrent requests in both directions", acceptance = "both peers complete concurrent requests over one session and every response resolves only its originating correlation without loss, swap, or duplicate completion", threats = [
    "THR-012",
  ] },
  { title = "reject invalid request correlations", acceptance = "unknown, reused, direction-swapped, and already-completed correlation identifiers return the manifest session error without dispatching or completing another request", threats = [
    "THR-009",
    "THR-012",
  ] },
  { title = "bound in-flight request ownership", acceptance = "the negotiated in-flight limit is never exceeded, excess requests receive a typed overload result, and cancellation or disconnect releases every correlation slot", threats = [
    "THR-010",
    "THR-012",
  ] },
]

[[scenario_set]]
owner_task = "T-G03-06"
gate = "G3"
priority = "P0"
first = 18
verification_id = "VERIFY-G03-06"
kind = "security"
cases = [
  { title = "reject replayed authentication material", acceptance = "replayed nonces, credential proofs, signatures, and complete prior handshake messages fail on a fresh TLS connection without consuming or admitting a credential", threats = [
    "THR-002",
  ] },
  { title = "reject channel and identity misbinding", acceptance = "wrong exporter, reflected role proof, wrong cluster, substituted public key, conflicting expected key, and identical endpoint identities all close the connection", threats = [
    "THR-003",
    "THR-004",
  ] },
  { title = "reject unsupported wire discriminators", acceptance = "unknown schema, kind, flag, reserved-bit, and selection identifiers fail closed with no decoder fallback, alternate offer, persistence, or dispatch", threats = [
    "THR-007",
    "THR-014",
  ] },
  { title = "reject noncanonical and malformed frames", acceptance = "text frames, length mismatches, trailing bytes, duplicate CBOR fields, noncanonical integers, invalid map order, and malformed identities are rejected before application dispatch", threats = [
    "THR-009",
    "THR-014",
  ] },
  { title = "enforce hostile and admission resource ceilings", acceptance = "concurrent source/global attempts, fixed-window edges, normalized-source aliases, full/expired bucket tables, rollback/discontinuity, oversized bodies, nesting, collections, fragmentation, and stalled authentication obey exact ADR-0006 count, rate, byte, allocation, task, reservation, and deadline bounds without consuming a rejected credential", threats = [
    "THR-001",
    "THR-009",
    "THR-010",
    "THR-012",
  ] },
]

[[scenario_set]]
owner_task = "T-G04-01"
gate = "G4"
priority = "P0"
first = 1
verification_id = "VERIFY-G04-01"
kind = "contract"
cases = [
  { title = "register transport extensions by canonical tag", acceptance = "transport registration accepts one valid owner-domain tag and rejects duplicate, malformed, reserved-domain, or conflicting registrations before use", threats = [
    "THR-014",
  ] },
  { title = "register discovery extensions without central switching", acceptance = "two independent discovery implementations resolve through the registry while duplicate and invalid registrations fail deterministically", threats = [
    "THR-014",
  ] },
  { title = "require authenticated transport results", acceptance = "no transport implementation can activate a session without returning the exact mutually authenticated cluster and peer identity binding expected by the caller", threats = [
    "THR-003",
    "THR-004",
    "THR-016",
  ] },
  { title = "preserve secure WSS behavior through registries", acceptance = "the built-in registered WSS transport passes the secure two-node join, request, disconnect, and member-reconnect regression without bypassing authentication", threats = [
    "THR-003",
    "THR-007",
  ] },
]

[[scenario_set]]
owner_task = "T-G04-02"
gate = "G4"
priority = "P0"
first = 5
verification_id = "VERIFY-G04-02"
kind = "integration"
cases = [
  { title = "bind endpoint candidates to authenticated identity", acceptance = "an observed address becomes a candidate only for the authenticated NodeId and cannot create, replace, or rebind an identity record", threats = [
    "THR-004",
    "THR-016",
  ] },
  { title = "merge endpoint candidates deterministically", acceptance = "duplicate and reordered observations yield one identity-scoped candidate set with stable provenance and no candidate crossing between peers", threats = [
    "THR-006",
    "THR-016",
  ] },
  { title = "expire stale endpoint candidates", acceptance = "the injected clock expires candidates at the exact configured boundary without deleting trust or affecting fresh candidates for the same identity", threats = [
    "THR-006",
  ] },
  { title = "readdress an authenticated member", acceptance = "after address replacement the peer reconnects at the new candidate using the same exact trusted key, while stale and attacker-supplied addresses cannot authorize a session", threats = [
    "THR-003",
    "THR-006",
    "THR-016",
  ] },
]

[[scenario_set]]
owner_task = "T-G04-03"
gate = "G4"
priority = "P0"
first = 9
verification_id = "VERIFY-G04-03"
kind = "concurrency"
cases = [
  { title = "resolve simultaneous crossed dials", acceptance = "all dial and authentication completion orderings choose the same single session owner from durable identities and session data", threats = [
    "THR-016",
  ] },
  { title = "drain the replaced authenticated session", acceptance = "the losing crossed-dial session stops accepting new work, completes or cancels owned work exactly once, and closes after the winner is active", threats = [
    "THR-010",
    "THR-016",
  ] },
  { title = "converge crossed dials after restart and readdress", acceptance = "restart, stale candidates, and concurrent readdressed dials converge to one authenticated session without oscillation, duplicate ownership, or address-based identity choice", threats = [
    "THR-006",
    "THR-016",
  ] },
]

[[scenario_set]]
owner_task = "T-G04-04"
gate = "G4"
priority = "P0"
first = 12
verification_id = "VERIFY-G04-04"
kind = "lifecycle"
cases = [
  { title = "enforce outbound queue count and byte bounds", acceptance = "queue admission atomically checks message count and encoded bytes, returns the typed overload error at either boundary, and never partially enqueues a rejected message", threats = [
    "THR-009",
    "THR-010",
    "THR-012",
  ] },
  { title = "terminate unresponsive peers with bounded keepalive", acceptance = "a peer missing the configured keepalive response is closed at the injected-clock deadline and cannot retain a task or queue slot indefinitely", threats = [
    "THR-010",
    "THR-012",
  ] },
  { title = "expire only genuinely idle sessions", acceptance = "idle sessions close at the exact deadline while authenticated traffic and owned in-flight work update activity without allowing a malicious peer to create unbounded lifetime or work", threats = [
    "THR-010",
    "THR-012",
  ] },
  { title = "complete bounded session shutdown", acceptance = "shutdown stops admission of new work, drains or cancels queued and in-flight work according to the manifest contract, closes transports, and leaves no session tasks or correlations", threats = [
    "THR-010",
  ] },
]

[[scenario_set]]
owner_task = "T-G04-05"
gate = "G4"
priority = "P0"
first = 16
verification_id = "VERIFY-G04-05"
kind = "integration"
cases = [
  { title = "persist an issuer-signed trust snapshot", acceptance = "a joiner verifies the issuer, cluster, revision, ordered bindings, version, and signature and durably persists every nonconflicting binding before alternate-peer trust", threats = [
    "THR-006",
    "THR-017",
  ] },
  { title = "disseminate the admitted binding to online members", acceptance = "every online member verifies and persists the exact admission grant and NodeId-to-key binding before acknowledging sync, and all trust views return the same pair", threats = [
    "THR-004",
    "THR-017",
  ] },
  { title = "catch up trust after an offline restart", acceptance = "a member offline during admission restarts, learns the exact binding through normal bounded trust anti-entropy, persists it, and then authenticates the new member", threats = [
    "THR-006",
    "THR-017",
  ] },
  { title = "reject conflicting trust evidence", acceptance = "invalid signatures, wrong clusters, untrusted issuers, reused admission identifiers, stale conflicting snapshots, and NodeId key substitutions fail closed without selecting a winner", threats = [
    "THR-004",
    "THR-006",
    "THR-012",
    "THR-017",
  ] },
]

[[scenario_set]]
owner_task = "T-G04-06"
gate = "G4"
priority = "P0"
first = 20
verification_id = "VERIFY-G04-06"
kind = "e2e"
cases = [
  { title = "reconnect through an alternate trusted peer", acceptance = "after disconnecting the issuer, the admitted node and another member independently load and verify each other's exact key and establish a fresh credential-free member session", threats = [
    "THR-003",
    "THR-006",
    "THR-017",
  ] },
  { title = "accept the grant-carrying reconnect path", acceptance = "before background sync completes, an alternate peer verifies and persists the presented issuer-signed grant and both endpoints establish reciprocal trust before session acceptance", threats = [
    "THR-004",
    "THR-012",
    "THR-017",
  ] },
  { title = "reconnect across outbound-only and readdressed paths", acceptance = "the issuer-disconnect, outbound-only, readdress, and crossed-dial public-facade targets each converge to one session bound to the expected peer key with no credential fallback", threats = [
    "THR-003",
    "THR-006",
    "THR-016",
  ] },
]

[[scenario_set]]
owner_task = "T-G05-01"
gate = "G5"
priority = "P0"
first = 1
verification_id = "VERIFY-G05-01"
kind = "security"
cases = [
  { title = "Reject descriptor signed by the wrong identity", acceptance = "Verification fails before storage when the descriptor signature does not match its bound NodeId and public key.", threats = [
    "THR-012",
  ] },
  { title = "Reject descriptor field mutation", acceptance = "Mutating any signed descriptor identity, endpoint, revision, or payload field invalidates the signature and leaves the stored descriptor unchanged.", threats = [
    "THR-006",
    "THR-012",
  ] },
  { title = "Enforce monotonic descriptor revisions", acceptance = "A descriptor update is accepted only at the exact next revision; stale, repeated, and skipped revisions cannot replace the current record.", threats = [
    "THR-006",
  ] },
  { title = "Preserve signed removal markers", acceptance = "A retained removal marker defeats reordered or replayed older descriptors and cannot be replaced without a valid newer signed revision.", threats = [
    "THR-002",
    "THR-006",
  ] },
  { title = "Validate descriptor compatibility vectors", acceptance = "Current and previous descriptor fixtures decode to the expected canonical values, while unknown versions and byte mutations fail closed.", threats = [
    "THR-006",
    "THR-009",
  ] },
]

[[scenario_set]]
owner_task = "T-G05-02"
gate = "G5"
priority = "P0"
first = 6
verification_id = "VERIFY-G05-02"
kind = "security"
cases = [
  { title = "Bound membership digest exchange", acceptance = "A normal anti-entropy tick emits no more than the configured digest entry and byte limits for the known member set.", threats = [
    "THR-009",
    "THR-010",
  ] },
  { title = "Repair missing membership records", acceptance = "Peers with missing signed descriptors obtain and verify the exact current records within bounded normal anti-entropy ticks.", threats = [
    "THR-006",
  ] },
  { title = "Replace stale membership records", acceptance = "A stale peer converges to the highest valid signed revision despite duplicate, reordered, and delayed digest messages.", threats = [
    "THR-002",
    "THR-006",
  ] },
  { title = "Reject dishonest membership digests", acceptance = "A malicious digest cannot install unsigned data, downgrade a valid revision, or cause requests beyond configured count and byte bounds.", threats = [
    "THR-009",
    "THR-012",
  ] },
]

[[scenario_set]]
owner_task = "T-G05-03"
gate = "G5"
priority = "P0"
first = 10
verification_id = "VERIFY-G05-03"
kind = "security"
cases = [
  { title = "Select a deterministic sparse neighbor set", acceptance = "Equal signed membership inputs produce the same bounded neighbor plan with no self-edge or duplicate peer.", threats = [
    "THR-008",
    "THR-012",
  ] },
  { title = "Keep reachability distinct from active topology", acceptance = "Directly reachable non-policy endpoints remain candidates only and do not create hidden sessions or full-mesh edges.", threats = [
    "THR-006",
    "THR-016",
  ] },
  { title = "Maintain the sparse cycle under churn", acceptance = "Join, leave, and endpoint expiry sequences restore the configured sparse cycle without exceeding the peer bound.", threats = [
    "THR-006",
    "THR-008",
    "THR-016",
  ] },
  { title = "Bound slow neighbor maintenance", acceptance = "Slow or nonresponsive candidates cannot exceed pending-connect, queue, timeout, or concurrent maintenance limits.", threats = [
    "THR-010",
    "THR-012",
  ] },
]

[[scenario_set]]
owner_task = "T-G05-04"
gate = "G5"
priority = "P0"
first = 14
verification_id = "VERIFY-G05-04"
kind = "security"
cases = [
  { title = "Enter recovery only for a missing policy edge", acceptance = "Recovery starts only after an expected policy neighbor is unavailable and never duplicates an active or pending session.", threats = [
    "THR-008",
    "THR-016",
  ] },
  { title = "Run exactly three policy recovery rounds", acceptance = "An unresolved recovery executes three and only three fixed policy rounds before changing strategy.", threats = [
    "THR-008",
    "THR-010",
  ] },
  { title = "Bound candidates within each fixed round", acceptance = "Each fixed round attempts every eligible policy candidate at most once and records a deterministic bounded attempt set.", threats = [
    "THR-008",
    "THR-012",
  ] },
  { title = "Fan out only after fixed rounds fail", acceptance = "All-known-peer fan-out begins only after the third unsuccessful fixed round and excludes self, duplicates, active peers, and expired endpoints.", threats = [
    "THR-006",
    "THR-008",
    "THR-016",
  ] },
  { title = "Stop recovery after replacement succeeds", acceptance = "The first authenticated policy-valid replacement cancels remaining attempts and emits no later recovery dial.", threats = [
    "THR-008",
    "THR-010",
  ] },
  { title = "Preserve recovery attempt bounds across restart", acceptance = "Restart reconstructs the durable attempt log without repeating completed rounds or exceeding the configured all-known candidate bound.", threats = [
    "THR-002",
    "THR-008",
    "THR-020",
  ] },
]

[[scenario_set]]
owner_task = "T-G05-05"
gate = "G5"
priority = "P0"
first = 20
verification_id = "VERIFY-G05-05"
kind = "security"
cases = [
  { title = "Match the exact simulated recovery count", acceptance = "Across 1,000 deterministic seeds, an unresolved recovery records exactly 3P+K attempts for policy width P and eligible known-peer count K.", threats = [
    "THR-008",
    "THR-012",
  ] },
  { title = "Converge sixteen nodes after policy-edge loss", acceptance = "The sixteen-node simulation restores the required sparse graph under delay, duplication, reorder, and partition healing without hidden edges.", threats = [
    "THR-008",
    "THR-010",
    "THR-016",
  ] },
  { title = "Count recovery through the public transport facade", acceptance = "The facade-only sixteen-node E2E observes exactly 3P+K transport attempts and cannot access private recovery state or inject a shortcut.", threats = [
    "THR-008",
    "THR-028",
  ] },
]

[[scenario_set]]
owner_task = "T-G05-06"
gate = "G5"
priority = "P0"
first = 23
verification_id = "VERIFY-G05-06"
kind = "security"
cases = [
  { title = "Validate induced fifteen-node readiness", acceptance = "Public views on nodes 0 through 14 agree on reciprocal trust, exact signed descriptors, and the required induced 28-edge graph before admission.", threats = [
    "THR-006",
    "THR-017",
    "THR-028",
  ] },
  { title = "Validate reciprocal node-fifteen trust", acceptance = "Nodes 0 through 14 expose node 15's exact NodeId-to-key grant and node 15 exposes all fifteen prior bindings through public queries.", threats = [
    "THR-006",
    "THR-017",
  ] },
  { title = "Validate signed descriptor readiness", acceptance = "All sixteen public membership views expose the exact expected descriptor revision and digest for every node.", threats = [
    "THR-006",
    "THR-012",
  ] },
  { title = "Validate the exact final topology", acceptance = "Every public topology view reports exactly the specified 32 undirected sessions, degree four, diameter three, and no extra or recovery edge.", threats = [
    "THR-008",
    "THR-016",
    "THR-028",
  ] },
  { title = "Survive the membership failure matrix", acceptance = "Partition healing, duplicate delivery, reorder, endpoint change, and process restart converge to identical trust, descriptor, and topology views within configured bounds.", threats = [
    "THR-002",
    "THR-006",
    "THR-010",
    "THR-016",
  ] },
  { title = "Reject readiness shortcuts", acceptance = "Readiness fails when any expected binding, descriptor, session, or edge is absent or extra, and the assertion uses only public facade observations.", threats = [
    "THR-017",
    "THR-028",
  ] },
  { title = "Track the metadata convergence SLO", acceptance = "The sixteen-node trend run records admission and descriptor completion from public observations and fails any sample exceeding 10,000 milliseconds.", threats = [
    "THR-010",
    "THR-028",
  ] },
]

[[scenario_set]]
owner_task = "T-G06-01"
gate = "G6"
priority = "P0"
first = 1
verification_id = "VERIFY-G06-01"
kind = "security"
cases = [
  { title = "Reject routed request signature mutation", acceptance = "Mutation of cluster, trace, source, destination, handler, feature requirements, payload, schema, or digest invalidates the source signature before route work.", threats = [
    "THR-009",
    "THR-018",
  ] },
  { title = "Reject fabricated attempt authorization", acceptance = "Only the source can authorize ordinals 0 through 3; wrong-key, out-of-range, reused-digest, and mutated authorization inputs fail closed.", threats = [
    "THR-002",
    "THR-012",
    "THR-018",
  ] },
  { title = "Enforce hop decrement and no fan-out", acceptance = "Each accepted DATA edge decrements a remaining hop count no greater than 15 exactly once and writes to one next hop only.", threats = [
    "THR-008",
    "THR-012",
    "THR-018",
  ] },
  { title = "Bound crash-free DATA writes", acceptance = "Property tests prove four authorized logical attempts produce at most 60 DATA edge writes with no forwarder-created ordinal or branch.", threats = [
    "THR-008",
    "THR-018",
  ] },
]

[[scenario_set]]
owner_task = "T-G06-02"
gate = "G6"
priority = "P0"
first = 5
verification_id = "VERIFY-G06-02"
kind = "security"
cases = [
  { title = "Key journal entries by authenticated ordinal", acceptance = "Journal lookup binds source, TraceId, digest, and ordinal before route or quota work and rejects a retained key with conflicting immutable content.", threats = [
    "THR-002",
    "THR-018",
  ] },
  { title = "Pin DATA and receipt hops durably", acceptance = "A committed DATA entry retains one next hop and a committed receipt entry retains one predecessor across route changes and restart.", threats = [
    "THR-008",
    "THR-020",
  ] },
  { title = "Recover only ambiguous pending writes", acceptance = "Commit or socket ambiguity may replay only the same WritePending ordinal; Sent entries and forwarders never trigger autonomous retries.", threats = [
    "THR-008",
    "THR-020",
  ] },
  { title = "Enforce journal quotas and compatibility", acceptance = "Count and reserved-byte quotas reject new traces atomically without evicting active records, while current and previous journal fixtures preserve identical ordinal and pinned-hop semantics.", threats = [
    "THR-009",
    "THR-012",
    "THR-019",
    "THR-020",
  ] },
]

[[scenario_set]]
owner_task = "T-G06-03"
gate = "G6"
priority = "P0"
first = 9
verification_id = "VERIFY-G06-03"
kind = "security"
cases = [
  { title = "Persist DATA state before forwarding", acceptance = "Faults at every commit and DATA-write boundary prove no DATA socket write occurs before its journal entry is durably committed.", threats = [
    "THR-008",
    "THR-020",
  ] },
  { title = "Persist receipt state before reverse forwarding", acceptance = "Faults at every receipt boundary prove an ACK or rejection is written only after its authenticated ordinal and predecessor are durably committed.", threats = [
    "THR-018",
    "THR-020",
  ] },
  { title = "Replay only the pinned pending edge", acceptance = "After restart, recovery writes only the same pending DATA or receipt ordinal to its persisted hop, never branches or recomputes, and receipt paths remain at most 15 edges.", threats = [
    "THR-008",
    "THR-018",
    "THR-020",
  ] },
]

[[scenario_set]]
owner_task = "T-G06-04"
gate = "G6"
priority = "P0"
first = 12
verification_id = "VERIFY-G06-04"
kind = "security"
cases = [
  { title = "Durably reject unsupported routed dispatch", acceptance = "An unknown handler or unavailable required feature commits RejectedUnsupported, signs a rejection, and never reaches Accepted or invokes a handler.", threats = [
    "THR-012",
    "THR-018",
    "THR-020",
  ] },
  { title = "Initiate handler dispatch at most once", acceptance = "Crash faults around Accepted and DispatchStarted prove the transition commits before invocation and no retained trace initiates its handler twice.", threats = [
    "THR-002",
    "THR-020",
  ] },
  { title = "Re-ACK retained duplicate acceptance", acceptance = "An identical retained duplicate receives a fresh valid ACK or rejection for its authenticated ordinal without changing the stored outcome or invoking the handler.", threats = [
    "THR-002",
    "THR-018",
  ] },
  { title = "Prioritize duplicate and conflict lookup under full quota", acceptance = "At full count and byte quota, identical retained traces are re-ACKed, conflicting digests fail closed, and only genuinely new traces receive overload.", threats = [
    "THR-012",
    "THR-018",
  ] },
]

[[scenario_set]]
owner_task = "T-G06-05"
gate = "G6"
priority = "P0"
first = 16
verification_id = "VERIFY-G06-05"
kind = "security"
cases = [
  { title = "Bound four-ordinal retry progression", acceptance = "The facade-only routed E2E emits only ordinals 0 through 3 with the fixed jitter ceilings, preserves the ordinal across restart, and never bursts overdue retries.", threats = [
    "THR-008",
    "THR-018",
    "THR-020",
    "THR-028",
  ] },
  { title = "Accept valid on-time and late ACKs", acceptance = "A valid retained ACK completes a pending request as Accepted, while an ACK after TimedOut records AcceptedLate without changing the caller's returned timeout.", threats = [
    "THR-018",
    "THR-019",
  ] },
  { title = "Apply the exact retention deadline", acceptance = "Source, forwarder, and destination terminal records become cleanup-eligible at first durable commit plus 24 hours, and retries, duplicates, re-ACKs, route changes, and state transitions never refresh the deadline.", threats = [
    "THR-002",
    "THR-019",
  ] },
  { title = "Retain records across clock uncertainty", acceptance = "Current-boot, prior-boot, unknown-commit, rollback, forward-jump, and discontinuity cases use a conservative elapsed-time lower bound and never delete a record early.", threats = [
    "THR-019",
    "THR-020",
  ] },
  { title = "Handle post-expiry delivery explicitly", acceptance = "Cleanup never evicts active records; an ACK for an absent expired source trace is ignored, and a duplicate after destination expiry is testably eligible for new acceptance and dispatch.", threats = [
    "THR-002",
    "THR-019",
  ] },
]

[[scenario_set]]
owner_task = "T-G07-01"
gate = "G7"
priority = "P0"
first = 1
verification_id = "VERIFY-G07-01"
kind = "security"
cases = [
  { title = "Checked HLC transitions and exhaustion", acceptance = "Exercise every ordered local and first-remote HLC branch, rollback, duplicate input, logical rollover, physical rollover, and terminal exhaustion; duplicates do not bump twice and ClockExhausted performs no write.", threats = [
    "THR-011",
    "THR-012",
  ] },
  { title = "Durable watermark and authenticated clock health", acceptance = "Fault every mutation, remote-acceptance, watermark, and response boundary; restart exposes old or new state atomically, while replayed, stale, unauthenticated, revoked, overflowing, or out-of-bound samples cannot advance time or authorize a local write.", threats = [
    "THR-002",
    "THR-011",
    "THR-020",
    "THR-021",
  ] },
]

[[scenario_set]]
owner_task = "T-G07-01"
gate = "G7"
priority = "P1"
first = 3
verification_id = "VERIFY-G07-01"
kind = "property"
cases = [
  { title = "Clock sample, slew, and quarantine boundaries", acceptance = "Test exact RTT, age, offset, uncertainty, discontinuity, slew, saturation, 5-second future-skew, and 24-hour horizon boundaries with distinct-peer voting and deterministic median tie ordering.", threats = [
    "THR-011",
    "THR-012",
  ] },
]

[[scenario_set]]
owner_task = "T-G07-02"
gate = "G7"
priority = "P0"
first = 4
verification_id = "VERIFY-G07-02"
kind = "security"
cases = [
  { title = "Signed state record validation", acceptance = "Mutating or omitting each signed field, changing cluster, writer, key, schema, body, or signing key, and exceeding decode bounds is rejected before quarantine, comparison, digesting, or persistence.", threats = [
    "THR-012",
  ] },
  { title = "Strict LWW comparator algebra", acceptance = "Generated equal-time, equal-writer, value, tombstone, and digest-tie records satisfy totality, antisymmetry, transitivity, commutativity, associativity, and idempotence for every permutation.", threats = [
    "THR-006",
    "THR-012",
  ] },
  { title = "Replay and equivocation convergence", acceptance = "Byte-identical replay is idempotent; same-writer same-HLC distinct bodies converge on the canonical digest maximum and emit exactly one bounded equivocation event without automatic cleanup or revoke.", threats = [
    "THR-002",
    "THR-012",
  ] },
]

[[scenario_set]]
owner_task = "T-G07-03"
gate = "G7"
priority = "P0"
first = 7
verification_id = "VERIFY-G07-03"
kind = "contract"
cases = [
  { title = "Atomic local state mutation", acceptance = "Put and delete conditionally commit the signed record and advanced watermark together; every injected crash or unknown outcome reopens to exactly the old or new pair and reconciles by transaction identity.", threats = [
    "THR-020",
    "THR-021",
  ] },
  { title = "Atomic remote state acceptance", acceptance = "First remote acceptance atomically stores winner or history and the merged watermark; duplicate, losing, quarantined, and conflicting records cannot produce partial history or watermark advancement.", threats = [
    "THR-002",
    "THR-006",
    "THR-020",
  ] },
  { title = "State precondition conflict isolation", acceptance = "Concurrent exact-version puts, deletes, and remote merges produce one commit and typed conflicts; failed or indeterminate operations never overwrite a newer winner or reuse a transaction ID with another digest.", threats = [
    "THR-020",
  ] },
]

[[scenario_set]]
owner_task = "T-G07-04"
gate = "G7"
priority = "P0"
first = 10
verification_id = "VERIFY-G07-04"
kind = "integration"
cases = [
  { title = "Bounded delta and digest synchronization", acceptance = "Duplicate, reordered, truncated, and paged deltas converge to the strict winner while every handler enforces record, byte, page, task, and queue bounds and rejects forged records before merge.", threats = [
    "THR-006",
    "THR-012",
  ] },
  { title = "Normal-tick anti-entropy repair", acceptance = "After loss, restart, readdress, quarantine maturation, or digest disagreement, the ordinary anti-entropy tick repairs accepted state without reconnect-only logic, unbounded fan-out, or false convergence acknowledgement.", threats = [
    "THR-006",
    "THR-011",
  ] },
]

[[scenario_set]]
owner_task = "T-G07-04"
gate = "G7"
priority = "P1"
first = 12
verification_id = "VERIFY-G07-04"
kind = "property"
cases = [
  { title = "State sync pagination convergence", acceptance = "For generated page boundaries and record permutations, repeated bounded sync reaches one stable digest and winner set, and a completed second pass transfers no authoritative changes.", threats = [
    "THR-006",
  ] },
]

[[scenario_set]]
owner_task = "T-G07-05"
gate = "G7"
priority = "P0"
first = 13
verification_id = "VERIFY-G07-05"
kind = "security"
cases = [
  { title = "Internal exact-tombstone cleanup CAS", acceptance = "An internal-only cleanup entry point requires the exact winning tombstone and non-default resurrection acknowledgement; stale expectations and races with newer put or delete return Conflict with no write, and no cleanup symbol is publicly exported.", threats = [
    "THR-020",
    "THR-023",
  ] },
  { title = "Internal cleanup atomic record removal", acceptance = "Fault every internal cleanup boundary and prove old-or-new removal of all local accepted winner, history, and quarantine records for the exact key, with no watermark reduction, absence version, replicated delete, unrelated mutation, or public API exposure.", threats = [
    "THR-021",
    "THR-023",
  ] },
  { title = "Internal cleanup resurrection semantics", acceptance = "Internal tests prove surviving peer tombstones restore deletion, all-copy cleanup permits an offline stale value to win only after acknowledgement, and maintenance, TTL, migration, compaction, and connectivity code cannot invoke cleanup authority.", threats = [
    "THR-006",
    "THR-023",
  ] },
]

[[scenario_set]]
owner_task = "T-G07-06"
gate = "G7"
priority = "P0"
first = 16
verification_id = "VERIFY-G07-06"
kind = "e2e"
cases = [
  { title = "Eight-plus-eight partition convergence", acceptance = "Two eight-node components mutate identical and disjoint keys, values, tombstones, and quarantined records; after reconnection, normal ticks converge all 16 nodes to exact winner and tombstone versions through duplicate, reorder, restart, and maturation faults.", threats = [
    "THR-006",
    "THR-011",
    "THR-012",
  ] },
  { title = "Membership ceiling functional replication and rejection", acceptance = "At configured limits below 1,024 and at the hard 1,024-member ceiling, concurrent final-slot admissions commit at most one member; every over-limit attempt returns InvalidInput/Overloaded before trust or membership persistence, while accepted membership, clock, digest, quarantine, and state workloads stay bounded and converge with no latency claim.", threats = [
    "THR-005",
    "THR-010",
    "THR-011",
    "THR-012",
  ] },
  { title = "Sixteen-node quantified state SLO", acceptance = "Only the exact 16-node ADR-0005 profile is latency-qualified: public readiness is healthy, no benchmark record is quarantined, and every measured exact winner or tombstone becomes readable by all 16 nodes within 10,000 milliseconds; larger-cluster results carry no quantified latency claim.", threats = [
    "THR-006",
    "THR-011",
  ] },
]

[[scenario_set]]
owner_task = "T-G08-01"
gate = "G8"
priority = "P0"
first = 1
verification_id = "VERIFY-G08-01"
kind = "contract"
cases = [
  { title = "All-record conditional storage parity", acceptance = "The JSON backend passes one reusable v2 contract covering snapshots, unsigned scans, base and per-key CAS, cross-family atomicity, receipts, capabilities, schema records, and durability for identity, trust, clock, membership, traces, state, and resources.", threats = [
    "THR-020",
    "THR-021",
  ] },
  { title = "Unknown transaction reconciliation", acceptance = "Every post-submission cancellation and I/O fault returns Unknown until locked reopen proves the exact receipt or absence; writes remain frozen, transaction IDs cannot change digest, and referenced receipts are not cleaned.", threats = [
    "THR-020",
    "THR-021",
  ] },
]

[[scenario_set]]
owner_task = "T-G08-01"
gate = "G8"
priority = "P1"
first = 3
verification_id = "VERIFY-G08-01"
kind = "contract"
cases = [
  { title = "Storage capability refusal", acceptance = "Opening with each unsupported durability, locking, scan, reconciliation, or migration requirement returns the exact typed capability error without mutation or silent semantic downgrade.", threats = [
    "THR-021",
    "THR-029",
  ] },
]

[[scenario_set]]
owner_task = "T-G08-02"
gate = "G8"
priority = "P0"
first = 4
verification_id = "VERIFY-G08-02"
kind = "contract"
cases = [
  { title = "redb conditional transaction contract", acceptance = "With feature redb enabled, the adapter passes the unchanged v2 storage contract and exposes identical logical records, scan order, conflicts, receipts, and reconciliation outcomes as JSON.", threats = [
    "THR-020",
    "THR-021",
  ] },
  { title = "redb feature isolation", acceptance = "Default, no-default, and redb builds prove redb code is absent when disabled and no concrete redb type appears in unconditional public signatures, errors, factories, or persisted logical records.", threats = [
    "THR-029",
  ] },
]

[[scenario_set]]
owner_task = "T-G08-02"
gate = "G8"
priority = "P1"
first = 6
verification_id = "VERIFY-G08-02"
kind = "platform-feature"
cases = [
  { title = "Storage feature powerset", acceptance = "Pinned cargo-hack check and test commands pass every supported default, no-default, JSON, redb, and combined feature selection with zero warnings and identical backend-neutral API signatures.", threats = [
    "THR-029",
  ] },
]

[[scenario_set]]
owner_task = "T-G08-03"
gate = "G8"
priority = "P0"
first = 7
verification_id = "VERIFY-G08-03"
kind = "integration"
cases = [
  { title = "redb commit crash classification", acceptance = "Subprocess kills at every pre-commit, commit, durability, result-delivery, and reopen boundary classify exact committed, aborted, or unknown outcomes and never expose a partial cross-family transaction.", threats = [
    "THR-020",
    "THR-021",
  ] },
  { title = "redb conflict and reconciliation integrity", acceptance = "Concurrent same-key and same-generation transactions produce exactly one commit; unknown outcomes reconcile to the matching receipt or proven absence, and transaction-ID digest conflicts fail closed.", threats = [
    "THR-020",
    "THR-021",
  ] },
  { title = "Provider intent and receipt cleanup parity", acceptance = "Create/delete intent faults on redb preserve every referenced key, reconcile provider and store outcomes, and remove receipts only after all owner, intent, migration, and unknown references are absent.", threats = [
    "THR-020",
    "THR-021",
  ] },
]

[[scenario_set]]
owner_task = "T-G08-04"
gate = "G8"
priority = "P0"
first = 10
verification_id = "VERIFY-G08-04"
kind = "security"
cases = [
  { title = "Migration graph validation", acceptance = "Registry construction rejects duplicate edges, cycles, ambiguous paths, unknown endpoints, missing decoders, implicit ordering, and downgrade requests before opening a write transaction.", threats = [
    "THR-022",
  ] },
  { title = "Transactional migration interruption", acceptance = "Every explicit migration edge is faulted before and after its commit point on JSON and redb; reopen exposes the complete old or new schema, indexes, receipts, and records with no mixed state.", threats = [
    "THR-021",
    "THR-022",
  ] },
  { title = "Migration replay and older-reader refusal", acceptance = "Applying every edge twice is a no-op only for the exact migration tag and implementation digest; mismatches and older readers fail closed without mutation or reverse migration.", threats = [
    "THR-002",
    "THR-022",
  ] },
]

[[scenario_set]]
owner_task = "T-G08-05"
gate = "G8"
priority = "P0"
first = 13
verification_id = "VERIFY-G08-05"
kind = "e2e"
cases = [
  { title = "Mixed JSON and redb convergence", acceptance = "A mixed-backend cluster restarts during trust, trace, state, tombstone, and resource synchronization and converges to byte-identical logical views without backend-specific public behavior.", threats = [
    "THR-006",
    "THR-021",
  ] },
  { title = "Mixed-backend restart integrity", acceptance = "Repeated graceful and killed restarts preserve identity bindings, keys, clock watermarks, descriptors, journals, tombstones, values, receipts, and schema state as old or new atomically.", threats = [
    "THR-021",
  ] },
]

[[scenario_set]]
owner_task = "T-G08-05"
gate = "G8"
priority = "P1"
first = 15
verification_id = "VERIFY-G08-05"
kind = "platform-feature"
cases = [
  { title = "Mixed-storage CI feature closure", acceptance = "Cargo-hack powersets and native workflow validation execute both mixed-storage tests with locked dependencies, no redb type leakage, no skipped backend contract, and zero warnings.", threats = [
    "THR-029",
  ] },
]

[[scenario_set]]
owner_task = "T-G09-01"
gate = "G9"
priority = "P0"
first = 1
verification_id = "VERIFY-G09-01"
kind = "security"
cases = [
  { title = "Label namespace ownership", acceptance = "Label parsing and mutation accept only bounded domain-qualified keys owned by the signer and reject spoofed domains, malformed names, reserved categories, and cross-owner updates before persistence.", threats = [
    "THR-012",
  ] },
  { title = "Reserved resource categories", acceptance = "Platform, feature, session, and application resource tags are category-checked; application writers cannot create or overwrite reserved internal resources.", threats = [
    "THR-012",
    "THR-029",
  ] },
  { title = "Per-key resource version order", acceptance = "Concurrent and reordered resource updates converge by exact owner, key, version, removal-marker, and digest rules; stale metadata cannot replace a newer accepted version.", threats = [
    "THR-006",
    "THR-012",
  ] },
  { title = "Resource vector compatibility", acceptance = "Current and previous resource fixtures round-trip canonically, reject signed-field mutation and unknown incompatible schemas, and preserve exact logical versions on JSON and redb.", threats = [
    "THR-006",
    "THR-021",
    "THR-022",
  ] },
]

[[scenario_set]]
owner_task = "T-G09-02"
gate = "G9"
priority = "P1"
first = 5
verification_id = "VERIFY-G09-02"
kind = "property"
cases = [
  { title = "Selector grammar bounds", acceptance = "Generated equality, inequality, set, non-set, existence, and non-existence selectors parse within documented token, key, value, nesting, and input limits and reject malformed or over-limit input without panic.", threats = [
    "THR-012",
  ] },
  { title = "Selector canonicalization", acceptance = "Equivalent whitespace and operand forms normalize to one canonical representation while distinct operators and escaped values remain distinguishable and round-trip exactly.", threats = [
  ] },
  { title = "Selector evaluator semantics", acceptance = "Property tests compare the implementation with a reference evaluator over absent, present, duplicate, reserved, and concurrently updated labels for every grammar operator.", threats = [
    "THR-006",
  ] },
  { title = "Converged selector parity", acceptance = "After concurrent label changes converge, every member returns the same deterministically ordered node IDs for each selector without treating stale local observations as authoritative.", threats = [
    "THR-006",
  ] },
]

[[scenario_set]]
owner_task = "T-G09-03"
gate = "G9"
priority = "P0"
first = 9
verification_id = "VERIFY-G09-03"
kind = "contract"
cases = [
  { title = "Atomic label batch mutation", acceptance = "A valid batch updates all requested label keys and the resource revision in one conditional JSON or redb transaction; any invalid item or stale revision writes nothing.", threats = [
    "THR-020",
  ] },
  { title = "Concurrent label mutation convergence", acceptance = "Concurrent batches against one revision produce one commit and typed conflicts, then normal metadata sync converges all members to the exact committed revision and digest.", threats = [
    "THR-006",
    "THR-020",
  ] },
  { title = "Exactly one post-commit label event", acceptance = "Each known committed label batch emits one event after durability; aborts emit none, unknown outcomes emit none until reconciliation, and replay never duplicates the committed event.", threats = [
    "THR-002",
    "THR-020",
  ] },
  { title = "Maintenance cannot erase labels", acceptance = "Disconnect, endpoint expiry, topology repair, anti-entropy, compaction, migration, and restart preserve label records and never emit a label-deletion event without an explicit committed mutation.", threats = [
    "THR-006",
  ] },
]

[[scenario_set]]
owner_task = "T-G09-04"
gate = "G9"
priority = "P0"
first = 13
verification_id = "VERIFY-G09-04"
kind = "security"
cases = [
  { title = "Durable authorization-only revoke", acceptance = "The typed revoke conditionally commits the exact subject/key before closing sessions and rejecting new online operations or raw grants; existing admitted members, signed state/resource/descriptor content, and unrelated members are not deleted or reinterpreted.", threats = [
    "THR-002",
    "THR-012",
    "THR-020",
  ] },
  { title = "Revoke crash, delayed-grant/content convergence, and reconnect denial", acceptance = "Fault every JSON/redb revoke, result, close, restart, grant, anti-entropy, and reconnect boundary: a locally revoked issuer's newly presented raw grant is rejected, but a grant durably accepted elsewhere before revoke later converges as an independently trusted binding through a current member's snapshot; valid historical/delayed content converges, abort preserves trust, and Unknown reconciles.", threats = [
    "THR-006",
    "THR-012",
    "THR-020",
    "THR-021",
  ] },
]

[[scenario_set]]
owner_task = "T-G09-05"
gate = "G9"
priority = "P0"
first = 15
verification_id = "VERIFY-G09-05"
kind = "security"
cases = [
  { title = "Sole public CleanupState operation", acceptance = "CleanupState is the only public cleanup operation or type, requires a domain-scoped key, exact winning tombstone version and digest, and non-default AcceptStaleReplicaResurrection acknowledgement; G7 cleanup machinery remains private.", threats = [
    "THR-020",
    "THR-023",
  ] },
  { title = "Public cleanup local atomicity", acceptance = "Through CleanupState, fault every JSON and redb boundary and prove old-or-new local removal of winner, history, and quarantine records without reducing the HLC watermark, creating an absence version, emitting a replicated delete, or changing unrelated metadata.", threats = [
    "THR-021",
    "THR-023",
  ] },
  { title = "Public cleanup risk and no-auto boundary", acceptance = "CleanupState reports local irreversible risk, conflicts on a newer winner, permits acknowledged offline stale resurrection only after all reachable tombstones are explicitly removed, and cannot be invoked by maintenance, TTL, migration, compaction, or connectivity paths.", threats = [
    "THR-006",
    "THR-023",
  ] },
]

[[scenario_set]]
owner_task = "T-G09-06"
gate = "G9"
priority = "P0"
first = 18
verification_id = "VERIFY-G09-06"
kind = "security"
cases = [
  { title = "Active leave identity rotation", acceptance = "A public active-leave request creates a fresh node ID and key, never reuses the old identity, and returns an outcome binding the exact old and new identities.", threats = [
    "THR-002",
    "THR-012",
  ] },
  { title = "Leave key-provider reconciliation", acceptance = "Fault every key-creation, binding, deletion-intent, provider deletion, and reconciliation boundary; no referenced key is deleted and every unknown provider or storage outcome remains quarantined.", threats = [
    "THR-020",
    "THR-021",
  ] },
  { title = "Old local cluster-state removal", acceptance = "After a committed leave, restart shows no old local trust, membership, session, trace, state, quarantine, resource, credential, or operation record while unrelated local data and the new identity remain intact.", threats = [
    "THR-021",
    "THR-023",
  ] },
  { title = "JSON and redb leave restart parity", acceptance = "Subprocess crashes across the leave transition reopen to a reconciled recoverable phase on both backends, never a mixed identity, duplicate active key, or silently restored old membership.", threats = [
    "THR-020",
    "THR-021",
  ] },
]

[[scenario_set]]
owner_task = "T-G09-07"
gate = "G9"
priority = "P0"
first = 22
verification_id = "VERIFY-G09-07"
kind = "security"
cases = [
  { title = "Informational platform resources", acceptance = "Windows, macOS, and Linux nodes publish bounded versioned platform and supported-feature resources matching native runtime facts, and stale or forged owner records are rejected.", threats = [
    "THR-006",
    "THR-012",
    "THR-029",
  ] },
  { title = "Session-scoped selected features", acceptance = "Selected-feature resources are keyed by peer pair and session generation, change on replacement or renegotiation, and disappear with the owning session without becoming node-wide capability claims.", threats = [
    "THR-006",
    "THR-029",
  ] },
  { title = "Resources never authorize negotiation", acceptance = "Mutating supported, platform, or selected-feature resources cannot enable a protocol, satisfy a required feature, authorize dispatch, or alter the signed handshake intersection.", threats = [
    "THR-012",
    "THR-029",
  ] },
  { title = "Facade-only explicit-operation E2E", acceptance = "An external-crate test performs labels, selection, revoke, CleanupState, events, and active leave using only public facade symbols and proves each committed operation persists across restart.", threats = [
    "THR-020",
    "THR-021",
  ] },
  { title = "G9 public API closure", acceptance = "The public API inventory exposes the manifest operations, including exactly one CleanupState surface, while excluding internal G7 cleanup authority, backend records, transaction receipts, wire types, runtime channels, and concrete JSON or redb types.", threats = [
    "THR-018",
    "THR-023",
    "THR-029",
  ] },
]
[[scenario_set]]
owner_task = "T-G10-01"
gate = "G10"
priority = "P0"
first = 1
verification_id = "VERIFY-G10-01"
kind = "compatibility"
cases = [
  { title = "Freeze current golden vectors", acceptance = "Golden-vector tests reproduce every manifest-listed current wire and persisted vector byte-for-byte with immutable schema, kind, and tag IDs.", threats = [
    "THR-014",
  ] },
  { title = "Preserve previous-reader compatibility", acceptance = "Every manifest-listed previous reader accepts its compatible current vector, and an omitted reader or fixture fails the compatibility suite.", threats = [
    "THR-007",
    "THR-014",
  ] },
  { title = "Complete JSON migration chain", acceptance = "The JSON migration suite executes every declared edge twice and proves each injected interruption reopens as exactly the old or new schema.", threats = [
    "THR-022",
  ] },
  { title = "Complete redb migration chain", acceptance = "The redb migration suite executes every declared edge twice and rejects partial-family state after committed, aborted, and unknown outcomes.", threats = [
    "THR-022",
  ] },
  { title = "Reject unsupported format versions", acceptance = "Unknown, ambiguous, cyclic, downgraded, and newer unsupported formats return only the frozen typed version errors without fallback decoding.", threats = [
    "THR-007",
    "THR-014",
    "THR-022",
  ] },
]

[[scenario_set]]
owner_task = "T-G10-02"
gate = "G10"
priority = "P0"
first = 6
verification_id = "VERIFY-G10-02"
kind = "mixed-binary"
cases = [
  { title = "Prior initiator negotiates current responder", acceptance = "A prior-version initiator and current responder select the signed optional-feature intersection and expose identical pair-scoped definitions.", threats = [
    "THR-007",
  ] },
  { title = "Current initiator negotiates prior responder", acceptance = "A current initiator and prior-version responder select the same signed intersection with platform and feature parity.", threats = [
    "THR-007",
  ] },
  { title = "Reject incompatible required features", acceptance = "Both initiator roles reject unknown, conflicting, or unsupported required features without retrying a weaker offer.", threats = [
    "THR-007",
    "THR-014",
  ] },
  { title = "Clean up session-scoped compatibility resources", acceptance = "Disconnect and replacement remove the old generation's selected-feature resources, and no node-wide label authorizes negotiation or dispatch.", threats = [
    "THR-007",
    "THR-014",
  ] },
]

[[scenario_set]]
owner_task = "T-G10-03"
gate = "G10"
priority = "P0"
first = 10
verification_id = "VERIFY-G10-03"
kind = "decoder-fuzz"
cases = [
  { title = "Replay reviewed decoder corpora", acceptance = "wire_decode, persisted_decode, and selector replay every approved manifest entry exactly once in filename order and reject malformed or over-bound inputs without panic.", threats = [
    "THR-009",
    "THR-024",
  ] },
  { title = "Run bounded decoder fuzz targets", acceptance = "Each activated decoder target completes its required uninterrupted live-fuzz budget with the pinned toolchain, ten-second input timeout, 4 GiB RSS bound, and no unscreened corpus promotion.", threats = [
    "THR-009",
    "THR-010",
    "THR-013",
    "THR-024",
    "THR-025",
  ] },
]

[[scenario_set]]
owner_task = "T-G10-04"
gate = "G10"
priority = "P0"
first = 12
verification_id = "VERIFY-G10-04"
kind = "state-machine-fuzz"
cases = [
  { title = "Fuzz admission state invariants", acceptance = "The admission target preserves single-subject generation binding, replay rejection, commit reconciliation, and secret-safe artifacts for every retained and live input.", threats = [
    "THR-009",
    "THR-013",
    "THR-024",
  ] },
  { title = "Fuzz feature-selection invariants", acceptance = "The feature_selection target rejects digest, dependency, conflict, limit, and required-feature mutations without downgrade or fallback.", threats = [
    "THR-007",
    "THR-009",
    "THR-024",
  ] },
  { title = "Fuzz routing state invariants", acceptance = "The routing target preserves ordinal, pinned-hop, persist-before-forward, duplicate re-ACK, retention, and quota invariants under every generated transition.", threats = [
    "THR-009",
    "THR-010",
    "THR-019",
    "THR-024",
  ] },
]

[[scenario_set]]
owner_task = "T-G10-05"
gate = "G10"
priority = "P0"
first = 15
verification_id = "VERIFY-G10-05"
kind = "observability"
cases = [
  { title = "Expose bounded public readiness and quiescence", acceptance = "Public facade tests observe exact trust, membership, topology, clock, quarantine, queue, task, storage, and resource baselines without private observer APIs.", threats = [
    "THR-010",
    "THR-028",
  ] },
  { title = "Redact production observations", acceptance = "Observability tests inject every forbidden credential, key, payload, address, path, selector, and hostile-string class and prove none enters logs, views, attestations, or replay arguments.", threats = [
    "THR-013",
    "THR-024",
  ] },
]

[[scenario_set]]
owner_task = "T-G10-06"
gate = "G10"
priority = "P0"
first = 17
verification_id = "VERIFY-G10-06"
kind = "soak"
cases = [
  { title = "Complete weekly eight-hour soak", acceptance = "The weekly churn, partition, slow-peer, clock, and resource suite runs uninterrupted for at least eight hours on one attested commit and records zero product failures or automatic retries.", threats = [
    "THR-010",
    "THR-025",
    "THR-026",
  ] },
  { title = "Complete release twenty-four-hour soak", acceptance = "The release soak runs uninterrupted for at least twenty-four hours on the immutable candidate SHA and lock digest with every failure retained in its attempt lineage.", threats = [
    "THR-010",
    "THR-025",
    "THR-026",
    "THR-027",
  ] },
  { title = "Return soak resources to baseline", acceptance = "After churn stops and quiescence completes, bounded tasks, queues, sessions, transactions, quarantine, file descriptors, and storage resources return to their declared baselines.", threats = [
    "THR-010",
    "THR-019",
  ] },
]

[[scenario_set]]
owner_task = "T-G10-07"
gate = "G10"
priority = "P0"
first = 20
verification_id = "VERIFY-G10-07"
kind = "native-ci"
cases = [
  { title = "Pass MSRV and stable quality matrices", acceptance = "Rust 1.97.1 and current stable complete Q with all targets, all features, locked dependencies, zero warnings, and attestations bound to the tested commit.", threats = [
    "THR-025",
    "THR-029",
  ] },
  { title = "Pass native feature powersets", acceptance = "Every declared native OS job checks and tests default, no-default, redb-only, all-features, and cargo-hack depth-one powersets without semantic weakening.", threats = [
    "THR-025",
    "THR-029",
  ] },
  { title = "Enforce dependency policy", acceptance = "Pinned cargo-deny and dependency-tree checks reject advisories, forbidden sources, unowned duplicates, alternate TLS providers, and feature leakage.", threats = [
    "THR-029",
  ] },
  { title = "Attest all canonical fuzz targets", acceptance = "CI records exactly one budget-complete attestation for each activated canonical fuzz target, including corpus digests, tool versions, limits, and uninterrupted status.", threats = [
    "THR-024",
    "THR-025",
    "THR-026",
  ] },
  { title = "Attest scheduled soak lineages", acceptance = "Nightly, weekly, and release workflows retain every soak attempt, predecessor digest, retry classification, duration, result, and resource baseline without product-failure supersession.", threats = [
    "THR-025",
    "THR-026",
  ] },
]

[[scenario_set]]
owner_task = "T-G10-08"
gate = "G10"
priority = "P0"
first = 25
verification_id = "VERIFY-G10-08"
kind = "public-api"
cases = [
  { title = "Freeze the functional public facade", acceptance = "cargo-public-api matches the approved 0.1 baseline exactly and reports no additional wire, persistence, runtime, backend, TLS, test-only, or workload-specific export.", threats = [
    "THR-014",
    "THR-029",
  ] },
  { title = "Prove semver and external-crate compatibility", acceptance = "cargo-semver-checks passes against the approved baseline and tests/public_api.rs exercises every supported facade signature solely as an external crate.", threats = [
    "THR-007",
    "THR-014",
  ] },
]

[[scenario_set]]
owner_task = "T-G10-09"
gate = "G10"
priority = "P0"
first = 27
verification_id = "VERIFY-G10-09"
kind = "evidence-validator"
cases = [
  { title = "Reject incomplete or under-budget evidence", acceptance = "Validator fixtures prove interrupted targets, missing attestations, reduced property or simulation counts, short fuzz or soak durations, and mismatched commit or lock digests fail preflight.", threats = [
    "THR-025",
    "THR-027",
  ] },
  { title = "Reject masked attempt lineages", acceptance = "Validator fixtures prove a product failure, missing predecessor, invalid retry classification, replacement run, or later successful rerun cannot supersede retained failed evidence.", threats = [
    "THR-026",
  ] },
  { title = "Reject invalid SLO and threat ledgers", acceptance = "Validator fixtures prove missing threats, sample IDs, raw values, profile constants, post-start exclusions, or cleanup status fail, while one complete synthetic ledger passes.", threats = [
    "THR-025",
    "THR-026",
    "THR-028",
  ] },
]

[[scenario_set]]
owner_task = "T-G10-10"
gate = "G10"
priority = "P0"
first = 30
verification_id = "VERIFY-G10-10"
kind = "oci-slo-harness"
cases = [
  { title = "Build external publish-false OCI harness", acceptance = "The publish-false workspace harness builds release slo-node and slo-controller images from an external path dependency at one exact source SHA and Cargo.lock digest.", threats = [
    "THR-027",
    "THR-028",
    "THR-029",
  ] },
  { title = "Enforce harness network and API isolation", acceptance = "Negative tests prove the controller has no data-network interface and node helpers cannot access private modules, storage internals, replication shortcuts, test-only features, or unscreened secrets.", threats = [
    "THR-013",
    "THR-028",
  ] },
  { title = "Qualify OCI profile preflight", acceptance = "Docker or Podman dry-run preflight verifies host, image, engine, cgroups, redb volumes, bridge routes, MTU, qdisc, security limits, all 64 final directions, and four throughput edges, rejecting every profile mismatch.", threats = [
    "THR-010",
    "THR-028",
  ] },
  { title = "Qualify workload readiness and cleanup", acceptance = "A disposable cluster executes the canonical warm-up order, exact induced and 32-edge final topologies, public readiness and quiescence predicates, bounded capture, graceful shutdown, and attempt-label-only cleanup.", threats = [
    "THR-010",
    "THR-028",
  ] },
]

[[scenario_set]]
owner_task = "T-G10-11"
gate = "G10"
priority = "P0"
first = 34
verification_id = "VERIFY-G10-11"
kind = "release-candidate-slo"
cases = [
  { title = "Create immutable publication candidate", acceptance = "Only after validator and harness qualification pass, one immutable candidate commit sets the intended 0.1 publication state and freezes its SHA, Cargo.lock, workflow, SBOM, and OCI image digests.", threats = [
    "THR-027",
    "THR-029",
  ] },
  { title = "Run exact candidate release matrices", acceptance = "Q, MSRV, stable, native features, compatibility, fuzz, threat, and uninterrupted release-soak matrices all pass on the immutable candidate with complete non-rerun-masked attestations.", threats = [
    "THR-025",
    "THR-026",
    "THR-027",
  ] },
  { title = "Record exactly 125 passing SLO samples", acceptance = "The candidate ledger contains exactly run-0 through run-4 with 5 admission, 20 descriptor, 80 put, and 20 tombstone samples, and every conservative elapsed value is at most 10000 milliseconds with no exclusion or replacement.", threats = [
    "THR-026",
    "THR-028",
  ] },
  { title = "Complete the candidate evidence ledger", acceptance = "The ledger binds every matrix and raw SLO timestamp, node offset, profile value, image, SHA, lock, predecessor, resource, shutdown, artifact, and cleanup record into one complete validated digest.", threats = [
    "THR-013",
    "THR-025",
    "THR-026",
    "THR-027",
    "THR-028",
  ] },
]

[[scenario_set]]
owner_task = "T-G10-12"
gate = "G10"
priority = "P0"
first = 38
verification_id = "VERIFY-G10-12"
kind = "publish"
cases = [
  { title = "Issue external eligibility token", acceptance = "After complete-provider-ledger validation, an external signed token records the exact candidate SHA, Cargo.lock digest, complete-ledger digest, package version 0.1.0, and eligible=true without entering the attested commit.", threats = [
    "THR-025",
    "THR-026",
    "THR-027",
  ] },
  { title = "Reject unauthorized release transitions", acceptance = "Guard tests reject an absent, malformed, expired, ineligible, wrong-ledger, wrong-lock, wrong-version, wrong-SHA, or unverifiable token and reject any checkout changed after token issuance.", threats = [
    "THR-027",
    "THR-029",
  ] },
  { title = "Tag and publish the attested candidate", acceptance = "The guarded workflow creates the 0.1.0 tag and publishes only the token-matched immutable candidate, then confirms the registry package and archive digests match the attested build.", threats = [
    "THR-027",
    "THR-029",
  ] },
]

[[e2e]]
id = "E2E-01"
title = "Secure join and credential rotation"
owner_task = "T-G03-04"
verification_id = "VERIFY-G03-04"
acceptance = "Join commits once; rotation does not disconnect members; reconnect uses key trust only."
threats = ["THR-001", "THR-002", "THR-003", "THR-005", "THR-007"]

[[e2e]]
id = "E2E-02"
title = "Outbound-only bidirectional session"
owner_task = "T-G04-06"
verification_id = "VERIFY-G04-06"
acceptance = "The listener sends typed API traffic over the dialer's authenticated full-duplex session."
threats = ["THR-003", "THR-010", "THR-016"]

[[e2e]]
id = "E2E-03"
title = "Crossed dial and readdress"
owner_task = "T-G04-06"
verification_id = "VERIFY-G04-06"
acceptance = "Exactly one owned session remains and identity/trust survive address replacement."
threats = ["THR-004", "THR-006", "THR-016"]

[[e2e]]
id = "E2E-04"
title = "Sixteen-node recovery"
owner_task = "T-G05-05"
verification_id = "VERIFY-G05-05"
acceptance = "Recovery performs exactly three preferred rounds plus one bounded fan-out and restores metadata."
threats = ["THR-006", "THR-008", "THR-010", "THR-028"]

[[e2e]]
id = "E2E-05"
title = "Three-hop ACK loss"
owner_task = "T-G06-05"
verification_id = "VERIFY-G06-05"
acceptance = "One durable acceptance occurs, duplicates re-ACK, and only ordinals zero through three exist."
threats = ["THR-002", "THR-008", "THR-018", "THR-019"]

[[e2e]]
id = "E2E-06"
title = "Eight-plus-eight partition merge"
owner_task = "T-G07-06"
verification_id = "VERIFY-G07-06"
acceptance = "Same and disjoint keys plus tombstones converge through normal anti-entropy after healing."
threats = ["THR-006", "THR-011", "THR-012"]

[[e2e]]
id = "E2E-07"
title = "JSON and redb crash restart"
owner_task = "T-G08-05"
verification_id = "VERIFY-G08-05"
acceptance = "Every backend reopens to old or new atomic state and converges to identical logical records."
threats = ["THR-020", "THR-021", "THR-022"]

[[e2e]]
id = "E2E-08"
title = "Labels revoke cleanup and leave"
owner_task = "T-G09-07"
verification_id = "VERIFY-G09-07"
acceptance = "Typed explicit operations persist and connectivity maintenance never erases durable content."
threats = ["THR-006", "THR-012", "THR-020", "THR-023"]

[[e2e]]
id = "E2E-09"
title = "Mixed binaries and feature resources"
owner_task = "T-G10-02"
verification_id = "VERIFY-G10-02"
acceptance = "Signed feature intersection controls behavior and informational resources never authorize it."
threats = ["THR-007", "THR-014", "THR-029"]

[[e2e]]
id = "E2E-10"
title = "Published sixteen-node SLO"
owner_task = "T-G10-11"
verification_id = "VERIFY-G10-11"
acceptance = "All 125 exact-candidate samples complete within ten seconds under ADR-0005 without exclusion."
threats = ["THR-025", "THR-026", "THR-027", "THR-028"]
